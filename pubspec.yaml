name: book_manager_app
description: Une application de gestion de livres avec BLoC

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # BLoC
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  # Réseau et stockage
  http: ^1.1.0
  sqflite: ^2.3.0
  path: ^1.8.3
  sqflite_common_ffi: ^2.3.0
  shared_preferences: ^2.2.1
  cached_network_image: ^3.2.3
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true